
<%= form_for(@excitement_page,:html => { :multipart => true }) do |f| %>
  <% if @excitement_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@excitement_page.errors.count, "error") %> prohibited this excitement_page from being saved:</h2>

      <ul>
      <% @excitement_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>



  <div class="actions">
    <%= f.submit nil, :class => 'btn btn-primary'  %>
  </div>


<% end %>






<script type="text/javascript">
  jQuery(function($) {
    var showUpload = $('#show-upload');
    var upload = $('#upload').hide();

    showUpload.click(function() {
      upload.show();
      showUpload.hide();
    })
  });
</script>