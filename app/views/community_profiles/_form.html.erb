<%= form_for(@community_profile) do |f| %>
  <% if @community_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@community_profile.errors.count, "error") %> prohibited this community_profile from being saved:</h2>

      <ul>
      <% @community_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bio %><br>
    <%= f.text_area :bio, :cols => "100", :rows => "10" %>
  </div>
  <div class="actions">
    <%= f.submit 'Save', :class => "btn btn-default" %>
    <%= link_to 'Cancel', @community_profile, :class => "btn btn-default" %>
  </div>
<% end %>
